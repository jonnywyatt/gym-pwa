# Goal 2 - UX improvements

## Bugs
- can't enter 0 as the offset weight for eg pull up

## Improvements
- if an exercise is finished in-workout, it can't currently be re-edited
- ability to delete one of the 2 default sets
- in workout, for each exercise / show the weight moved during the last workout
- date formatting - only show year if it wasn't this year. Change date format to `16 Feb @ 3:45pm`
- in workout, make timer / total weight a sticky bar
- if user enters weight / time / reps then set can be marked complete, no need for checkbox
