# Goal 10: body area trends

On the sessions page, a new line graph to show the relative contribution of each body area to sessions, over time. Each body area should have its own line and colour, using the colour mappings from the session summary body area breakdown. Offer the same time period switcher as the existing graphs.
