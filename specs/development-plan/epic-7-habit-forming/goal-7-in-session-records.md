# Goal 7: in session records

Show the user feedback if they set a record during a session.

Examples:
- highest total weight moved for a session
- highest total weight moved for a set (weight x reps)
- highest weght moved for an exercise (eg in a single rep)
- highest total weight moved for all sets for an exercise

Also all of the above but for time, or time + weight.
