# Goal 6 - sessions page

Refactor the /sessions page - it should use the 'session calendar' component from the dashboard to show workouts for each calendar month. Above each month show the summary count for each routine type.

The subheading for each month should use the uppercase style.
