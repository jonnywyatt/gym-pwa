# Goal 1: Abs Routine

Add a new RecordSetsType - 'REPS'. If an exercise has this type, in the workout page, for each set show only one input for the number of reps.

Next, add the new exercises in ./goal-1-abs-routine.md to the database by adding to api/prisma/seeds/exercises.ts

Then, add a new routine to api/prisma/seeds/routines.ts called 'Abs', and add the new exercises to it.
