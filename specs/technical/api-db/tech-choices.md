# Technology choices for REST API / database:
- Express (REST API framework)
- PostgreSQL (Railway-hosted relational database)
- Prisma (type-safe ORM and schema definition)
    - @prisma/client (database queries)
- google-auth-library (verify OAuth tokens from frontend)
- jsonwebtoken (generate access/refresh tokens for authenticated API requests)
