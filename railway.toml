# Node version specified in .node-version file (Node 24 required for Prisma 7)
[build]
builder = "nixpacks"
buildCommand = "npm install && npm run prisma:generate -w api && npm run build:api"

[deploy]
startCommand = "npm run start -w api"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[variables]
CORS_ORIGIN = "https://gym-pwa-321.netlify.app"
